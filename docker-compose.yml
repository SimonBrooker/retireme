services:
  retireme:
    build: .
    image: retireme:latest
    ports:
      - "5000:5000"
    volumes:
      - ./data:/data
    environment:
      - SECRET_KEY=${SECRET_KEY}
      - DATABASE_PATH=/data/retirement.db
      - SECURE_COOKIES=${SECURE_COOKIES:-false}
    restart: unless-stopped
